backend-db
	- check the correct config of user db (mysql_user)
	- use encrypting algorithm to secure users passwords
	- search for API to simplify the user registration logic

backend-server
	- create function to retrive data from db, based on user input
	- search for API to simplify the user input parsing and password encryption

frontend-client
	- 