backend-db
	DONE

backend-users
	- Done basic route for /create /login /profile /delete
	- Can add some advanced routes like /change-password etc... (not mandatory)

frontend-client
	- Make sure the container runs smoothly at start up