backend-db
	- users: DONE
	- Stories: create a table for the stories (title, content, comments, stars, owners, total-reads)

backend-users
	- Done basic route for /create /login /profile /delete
	- Can add some advanced routes like /change-password etc... (not mandatory)

backend-stories
	- create the stories microservice using java/springboot

frontend-client
	- Make sure the container runs smoothly at start up