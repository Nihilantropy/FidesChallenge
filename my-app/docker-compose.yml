services:
  # Backend Server
  backend-server:
    env_file:
      - ./backend/server/.env
    build:
      context: ./backend/server
      dockerfile: Dockerfile
    command: npm run start-watch
    ports:
      - "3000:3000"
      - "9229:9229"
      - "9230:9230"
    volumes:
      - ./backend/server/src:/code/src:rw
      - ./backend/server/package.json:/code/package.json
      - ./backend/server/package-lock.json:/code/package-lock.json
      - back-notused:/opt/app/node_modules
    networks:
      - client-server
      - server-db
    depends_on:
      db:
        condition: service_healthy

  # Database (MySQL)
  db:
    env_file:
      - ./backend/db/.env
    build:
      context: ./backend/db
      dockerfile: Dockerfile
    restart: always
    volumes:
      - db-data:/var/lib/mysql
      - ./backend/db/initdb.d:/docker-entrypoint-initdb.d
    networks:
      - server-db
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      retries: 5
      start_period: 30s
      timeout: 5s

  # Frontend Client
  frontend-client:
    env_file:
      - ./frontend/client/myapp/.env
    build:
      context: ./frontend/client/myapp
      dockerfile: Dockerfile
    ports:
      - "8081:8081"
      - "19006:19006"
      - "19001:19001"
      - "19002:19002"
    volumes:
      - ./frontend/client/myapp/src:/code/src
      - /code/node_modules
    networks:
      - client-server
    depends_on:
      backend-server:
        condition: service_healthy

# Networks configuration
networks:
  client-server:
    driver: bridge
  server-db:
    driver: bridge

# Volumes
volumes:
  back-notused:
  db-data:
