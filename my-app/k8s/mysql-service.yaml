apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: database-ns
  labels:
    app: mysql-app
spec:
  selector:
    app: mysql-app
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
  type: ClusterIP # Ensures internal cluster communication
